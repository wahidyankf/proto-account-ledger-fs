# Account Ledger

Specifications for the in-memory account ledger. The ledger ships one owner today, a command-line interface.

## Directory Map

- [CLI](cli/README.md) — the specification corpus for `account-ledger-cli`.
