# Account Ledger CLI — Specification Corpus

The as-built description of [`apps/account-ledger-cli`](../../../../apps/account-ledger-cli/README.md). Its behaviour,
the command's input, the text it writes, and its exit code, is specified by the application's plain pytest tests.

## Directory Map

- [Architecture](architecture.md) — the as-built system: its context, the one executable, its interior, and its domain
  model.
