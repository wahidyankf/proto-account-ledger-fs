# Account Ledger CLI — Specification Corpus

The single source of truth for what [`apps/account-ledger-cli`](../../../../apps/account-ledger-cli/README.md) does. A
scenario here defines the command's input, the text it writes to standard output, and its exit code.

## Directory Map

- [Architecture](architecture.md) — the as-built system: its context, the one executable, and its interior.
- [Behaviours](behaviours/README.md) — the Gherkin corpus both test projects execute.
