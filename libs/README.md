# Libraries

Reusable libraries consumed by the applications in [apps/](../apps/). Applications depend on libraries, never the
reverse, and libraries never depend on each other in a cycle; see
[monorepo layout](../repo-governance/conventions/structure/monorepo-layout.md).

There are no libraries yet.
